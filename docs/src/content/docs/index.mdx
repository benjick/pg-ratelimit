---
title: pg-ratelimit
description: PostgreSQL-backed rate limiting for Node.js. No Redis required.
template: splash
head:
  - tag: title
    content: pg-ratelimit | PostgreSQL-backed rate limiting for Node.js
hero:
  tagline: Rate limiting backed by your existing Postgres. No Redis, no extra infrastructure.
  actions:
    - text: Get Started
      link: /pg-ratelimit/getting-started/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/benjick/pg-ratelimit
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

```typescript
import { Pool } from "pg";
import { Ratelimit } from "pg-ratelimit";

const pool = new Pool();
const ratelimit = new Ratelimit({
  pool,
  prefix: "api",
  limiter: Ratelimit.fixedWindow(10, "60s"),
});

const { success } = await ratelimit.limit("user:123");
if (!success) {
  return new Response("Too Many Requests", { status: 429 });
}
```

<CardGrid>
  <Card title="Zero runtime dependencies" icon="seti:config">
    Just `pg` as a peer dependency. No Redis, no extra services - use the Postgres you already have.
  </Card>
  <Card title="Three algorithms" icon="list-format">
    Fixed window, sliding window, and token bucket. Pick the right tradeoff for your use case.
  </Card>
  <Card title="Serverless-safe" icon="rocket">
    No background processes or singletons. Probabilistic inline cleanup with no long-lived state.
    Works in Lambda, Vercel, and other serverless platforms.
  </Card>
  <Card title="Upstash-compatible API" icon="random">
    Same `limit()`, `blockUntilReady()`, `getRemaining()`, and `resetUsedTokens()` surface. Migrate
    with minimal code changes.
  </Card>
</CardGrid>

## Why pg-ratelimit?

Most rate limiting libraries require Redis or a dedicated service. If you already run PostgreSQL, you already have everything you need.

- **No extra infrastructure** - zero runtime dependencies beyond `pg`. No Redis, no sidecars, no new thing to monitor.
- **Durable mode** - logged tables survive crashes, useful for billing and quota enforcement.
- **UNLOGGED tables** - skip WAL writes to reduce overhead. In benchmarks a single node handles ~6,700 req/s, or ~10,300 req/s with in-memory blocking enabled.
